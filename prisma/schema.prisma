// Model schema for users and links (optional if integrating DB)
model User {
  id    String   @id @default(cuid())
  email String   @unique
  links Link[]
}

model Link {
  id     String @id @default(cuid())
  url    String
  userId String
  user   User   @relation(fields: [userId], references: [id])
}
